import { NavigationContainer } from "@react-navigation/native";

import TapBar from "./TapBar";

const Nav = () => {
  return (
    <NavigationContainer>
      <TapBar />
    </NavigationContainer>
  );
};

export default Nav;
